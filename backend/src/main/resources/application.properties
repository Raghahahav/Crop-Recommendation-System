spring.application.name=smart-crop-recommendation
server.port=8080

# Increase multipart and JSON defaults only if needed later
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
